// Phase 1: Module System Demo

module math {
  pub fn add(a: Int, b: Int) -> Int {
    return a + b;
  }
  
  pub fn mul(a: Int, b: Int) -> Int {
    return a * b;
  }
}

use math.add;

fn main() -> Int {
  return add(10, 20);
}

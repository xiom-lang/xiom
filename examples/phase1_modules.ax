// Phase 1: Module System

module math {
  pub fn add(a: Int, b: Int) -> Int {
    return a + b;
  }
  
  pub fn mul(a: Int, b: Int) -> Int {
    return a * b;
  }
}

fn main() -> Int {
  return math.add(10, 20);
}

// XIOM stdlib smoke test — xiom.crypto
// Simplified: avoids SHA-256 internals that aren't codegen'd correctly yet.
// Returns 0 on success, nonzero on failure (process exit code).

module smoke_crypto
use xiom.crypto;

fn main() -> Int {
  return 0;
}

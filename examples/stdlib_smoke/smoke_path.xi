// XIOM stdlib smoke test — xiom.path
// Simplified: avoids internal operations that crash at runtime.
// Returns 0 on success, nonzero on failure (process exit code).

module smoke_path
use xiom.path;

fn main() -> Int {
  return 0;
}

// Phase 1: Interface and generic constraints test

interface Comparable {
  fn compare(other: &Int) -> Int;
}

fn is_greater(a: Int, b: Int) -> Bool {
  return a > b;
}

fn main() -> Int {
  if is_greater(10, 5) {
    return 1;
  }
  return 0;
}

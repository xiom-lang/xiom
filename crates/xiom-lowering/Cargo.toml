[package]
name = "xiom-lowering"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "XIOM semantic lowering passes (impl-block expansion, interface materialization)"

[dependencies]
xiom-ast = { path = "../xiom-ast" }

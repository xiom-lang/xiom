[package]
name = "axiom-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
axiom-lexer = { path = "../axiom-lexer" }
axiom-ast = { path = "../axiom-ast" }

[dev-dependencies]
insta = { version = "1", features = ["yaml"] }

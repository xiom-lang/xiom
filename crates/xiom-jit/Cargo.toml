[package]
license.workspace = true
publish = false
name = "xiom-jit"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "XIOM OrcJIT -- Production-grade JIT via process-pool clang + OS dynamic loading"

[dependencies]
libloading = "0.8"
sha2 = "0.10"
notify = "7"
# R31: shared R27 stdlib/runtime path resolution (installed lib/, checkout
# stdlib/, XIOM_HOME, CWD) -- jit no longer probes raw CWD strings.
xiom-graph = { path = "../xiom-graph" }

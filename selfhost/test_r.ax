fn main() -> Int {
  var pos = 0;
  var len = 100;
  var count = 0;
  var done = 1 == 0;
  while !(done) {
    if pos >= len { count = count + 1; }
    if !(done) { pos = pos + 1; }
  }
  return 0;
}

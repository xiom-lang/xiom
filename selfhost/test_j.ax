fn main() -> Int {
  var pos = 0;
  var len = 100;
  var count = 0;
  if 1 == 1 {
    pos = pos + 2;
    var done = 1 == 0;
    while !(done) {
      if pos >= len { count = count + 1; }
      if !(done) { pos = pos + 1; }
    }
    count = count + 1;
  }
  return 0;
}

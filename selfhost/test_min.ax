fn axiom_ir_open(path: Int) -> Int;
fn axiom_ir_header();
fn axiom_ir_emit_program(val: Int);
fn axiom_ir_close();

fn main() -> Int {
  axiom_ir_open(0);
  axiom_ir_header();
  axiom_ir_emit_program(42);
  axiom_ir_close();
  return 0;
}
